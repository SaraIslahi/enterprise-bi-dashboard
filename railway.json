{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "cd bi_webapp && python manage.py collectstatic --noinput || true && gunicorn bi_webapp.wsgi:application --bind 0.0.0.0:$PORT --log-level debug"
  }
}
